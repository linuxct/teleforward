package space.linuxct.teleforward.designsystem

import androidx.compose.material3.Typography

/**
 * App typography. Uses the Material 3 defaults for Wave 0; a Wave-1 UI pass can customise
 * individual text styles here without touching call sites.
 */
val AppTypography = Typography()
